# Package list

Below is a list of my packages that come in the standard debian apt repos and thus don't require wrapper scripts

```sh
apt install git net-tools sudo dnsutils make curl wget bat btop yq zsh gh
```